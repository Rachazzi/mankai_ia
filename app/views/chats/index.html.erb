<div class="container">
<div class="mb-4">
  <% if @chats.any? %>
    <ul>
      <% @chats.each do |chat| %>
        <li><%= link_to chat.title, chat_path(chat) %></li>
      <% end %>
    </ul>
  <% else %>
    Pas de discussion actuellement pour ce manga.
  <% end %>
</div>

<%= simple_form_for [@manga, @chat] do |f| %>
  <%= f.button :submit, "New chat", class: "btn btn-magic" %>
<% end %>
</div>
